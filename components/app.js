import { Component } from 'preact';
import Header from './header';

export default class App extends Component {
    render() {
        return (
            <div class="app">
                <Header />
            </div>
        );
    }
}
